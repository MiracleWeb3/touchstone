name: selftest

on:
  push:
    branches: [main]
  pull_request:

jobs:
  gate:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: build (warnings are errors)
        run: c++ -std=c++20 -O2 -Wall -Wextra -Werror -DTOUCHSTONE_SELFTEST -o /tmp/touchstone src/*.cpp

      - name: selftest
        run: /tmp/touchstone --selftest

      - name: gate benchmark — planted fake-dones caught, clean turns left alone
        run: /tmp/touchstone --bench bench/fixtures

      - name: fails open on garbage
        run: |
          echo 'not json' | /tmp/touchstone
          echo '{}'       | /tmp/touchstone
          printf ''       | /tmp/touchstone
          test -z "$(echo 'not json' | /tmp/touchstone)"

      - name: a missing transcript is silent, not fatal
        run: |
          echo '{"transcript_path":"/nope/none.jsonl","session_id":"x"}' | /tmp/touchstone
          test -z "$(echo '{"transcript_path":"/nope/none.jsonl"}' | /tmp/touchstone)"

      - name: manifests parse
        run: python3 -c "import json;[json.load(open(p)) for p in ['.claude-plugin/plugin.json','.claude-plugin/marketplace.json','hooks/hooks.json']]"

      - name: every file inside the 300-line rule
        run: |
          for f in src/*.cpp src/*.hpp src/*.inc; do
            n=$(wc -l < "$f"); echo "$f: $n"; test "$n" -le 300
          done
